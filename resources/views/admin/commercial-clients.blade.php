@extends('admin.layout')
@section('content')
<div class="d-flex justify-content-between align-items-center mb-5"><div><div class="text-uppercase text-muted fs-8 fw-bold">Commercial</div><h2 class="fs-2 fw-bold mb-1">Clients</h2><p class="text-muted mb-0">Gérez les informations administratives et coordonnées des clients.</p></div><div class="d-flex gap-2"><a href="{{ route('admin.commercial') }}" class="btn btn-light">Retour à l’interface principale</a><button class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#newClientModal">Nouveau client</button></div></div>
@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif
@if($errors->any())<div class="alert alert-danger">{{ $errors->first() }}</div>@endif
<div class="modal fade" id="newClientModal" tabindex="-1" aria-hidden="true"><div class="modal-dialog modal-lg"><div class="modal-content"><form method="POST" action="{{ route('admin.commercial.clients.store') }}">@csrf<div class="modal-header"><h5 class="modal-title">Nouveau client</h5><button type="button" class="btn-close" data-bs-dismiss="modal"></button></div><div class="modal-body row g-4">@foreach([['name','Client','required'],['responsible_name','Nom du responsable',''],['phone','Téléphone','required'],['email','Email','required'],['city','Ville',''],['tax_id','Compte contribuable',''],['tax_regime',"Régime d'imposition",'']] as [$field,$label,$required])<div class="col-md-6"><label class="form-label">{{ $label }} @if($required)<span class="text-danger">*</span>@endif</label><input name="{{ $field }}" class="form-control" {{ $required }}></div>@endforeach<div class="col-12"><label class="form-label">Adresse</label><textarea name="address" class="form-control" rows="2"></textarea></div></div><div class="modal-footer"><button type="button" class="btn btn-light" data-bs-dismiss="modal">Fermer</button><button class="btn btn-primary">Enregistrer</button></div></form></div></div></div>
<div class="card border-0"><div class="card-header border-0"><h3 class="card-title">Liste des clients</h3></div><div class="card-body table-responsive"><table class="table align-middle"><thead><tr><th>Client</th><th>Responsable</th><th>Téléphone</th><th>Email</th><th>Ville</th><th>Compte contribuable</th><th>Actions</th></tr></thead><tbody>@forelse($clients as $client)<tr><td>{{ $client->name }}</td><td>{{ $client->responsible_name ?: '-' }}</td><td>{{ $client->phone }}</td><td>{{ $client->email }}</td><td>{{ $client->city ?: '-' }}</td><td>{{ $client->tax_id ?: '-' }}</td><td><div class="dropdown"><button class="btn btn-sm btn-light" data-bs-toggle="dropdown">...</button><div class="dropdown-menu dropdown-menu-end"><button class="dropdown-item" data-bs-toggle="modal" data-bs-target="#client{{ $client->id }}">Modifier</button><form method="POST" action="{{ route('admin.commercial.clients.destroy',$client) }}" onsubmit="return confirm('Supprimer ce client ?')">@csrf @method('DELETE')<button class="dropdown-item text-danger">Supprimer</button></form></div></div></td></tr><div class="modal fade" id="client{{ $client->id }}"><div class="modal-dialog modal-lg"><div class="modal-content"><form method="POST" action="{{ route('admin.commercial.clients.update',$client) }}">@csrf @method('PUT')<div class="modal-header"><h5>Modifier le client</h5></div><div class="modal-body row g-3">@foreach(['name'=>'Client','responsible_name'=>'Nom du responsable','phone'=>'Téléphone','email'=>'Email','city'=>'Ville','tax_id'=>'Compte contribuable','tax_regime'=>"Régime d'imposition"] as $field=>$label)<div class="col-md-6"><label class="form-label">{{ $label }}</label><input name="{{ $field }}" value="{{ $client->$field }}" class="form-control" required="{{ in_array($field,['name','phone','email']) ? 'required' : '' }}"></div>@endforeach<div class="col-12"><label class="form-label">Adresse</label><textarea name="address" class="form-control">{{ $client->address }}</textarea></div></div><div class="modal-footer"><button class="btn btn-primary">Enregistrer</button></div></form></div></div></div>@empty<tr><td colspan="7" class="text-center text-muted">Aucun client enregistré.</td></tr>@endforelse</tbody></table></div></div>
@endsection
