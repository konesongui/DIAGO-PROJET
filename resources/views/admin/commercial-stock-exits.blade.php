@extends('admin.layout')
@section('content')
<div class="d-flex justify-content-between align-items-center mb-5"><div><div class="text-uppercase text-muted fs-8 fw-bold">Commercial / Stock</div><h2 class="fs-2 fw-bold mb-1">Sorties de stock</h2><p class="text-muted mb-0">Consultez les articles sortis du stock.</p></div><div class="d-flex gap-2"><a href="{{ route('admin.commercial') }}" class="btn btn-light">Retour</a><a href="{{ route('admin.commercial.stock-exits.create') }}" class="btn btn-primary">Nouvelle sortie de stock</a></div></div>
@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif
<div class="card border-0"><div class="card-header border-0"><h3 class="card-title">Sorties enregistrées</h3></div><div class="card-body table-responsive"><table class="table align-middle"><thead><tr><th>Date</th><th>Lignes</th><th>Valeur sortie</th></tr></thead><tbody>@forelse($exits as $exit)<tr><td>{{ $exit->exit_date->format('d/m/Y') }}</td><td>{{ $exit->lines->count() }}</td><td>{{ money($exit->total_value) }}</td></tr>@empty<tr><td colspan="3" class="text-center text-muted">Aucune sortie enregistrée.</td></tr>@endforelse</tbody></table></div></div>
@endsection
