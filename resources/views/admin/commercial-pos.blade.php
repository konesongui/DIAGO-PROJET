@extends('admin.layout')
@section('content')
<div class="d-flex justify-content-between align-items-center mb-5"><div><div class="text-uppercase text-muted fs-8 fw-bold">Commercial</div><h2 class="fs-2 fw-bold mb-1">Point de vente</h2><p class="text-muted mb-0">Suivez les ventes enregistrées à la caisse.</p></div><a href="{{ route('admin.commercial.pos.create') }}" class="btn btn-primary"><i class="ki-duotone ki-plus fs-2 me-2"></i>Nouveau</a></div>
@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif
@if($errors->any())<div class="alert alert-danger">{{ $errors->first() }}</div>@endif
<div class="card border-0"><div class="card-header border-0"><h3 class="card-title">Ventes récentes</h3></div><div class="card-body table-responsive"><table class="table align-middle"><thead><tr><th>Date</th><th>Client</th><th>Articles</th><th class="text-end">Total</th><th>Payé</th><th>Rendu</th><th>Mode</th><th>Statut</th><th>Action</th></tr></thead><tbody>@forelse($sales as $sale)<tr><td>{{ $sale->created_at->format('d/m/Y H:i') }}</td><td>{{ $sale->client_name ?: 'Client comptoir' }}</td><td>{{ count($sale->lines ?: []) }}</td><td class="text-end">{{ number_format($sale->total,0,',',' ') }} XOF</td><td class="text-end">{{ number_format($sale->paid_amount,0,',',' ') }} XOF</td><td class="text-end">{{ number_format($sale->change_amount,0,',',' ') }} XOF</td><td>{{ $sale->payment_method === 'cash' ? 'Espèces' : 'Banque' }}</td><td><span class="badge badge-light-{{ $sale->status === 'cancelled' ? 'danger' : 'success' }}">{{ $sale->status === 'cancelled' ? 'Annulée' : 'Terminée' }}</span></td><td><div class="dropdown"><button class="btn btn-sm btn-light" data-bs-toggle="dropdown">...</button><ul class="dropdown-menu dropdown-menu-end"><li><a class="dropdown-item" href="{{ route('admin.commercial.pos.show',$sale) }}">Voir</a></li><li><a class="dropdown-item" href="{{ route('admin.commercial.pos.edit',$sale) }}">Modifier</a></li><li><form method="POST" action="{{ route('admin.commercial.pos.cancel',$sale) }}">@csrf<button class="dropdown-item text-warning">Annuler</button></form></li><li><form method="POST" action="{{ route('admin.commercial.pos.destroy',$sale) }}" onsubmit="return confirm('Supprimer cette vente ?')">@csrf @method('DELETE')<button class="dropdown-item text-danger">Supprimer</button></form></li></ul></div></td></tr>@empty<tr><td colspan="9" class="text-center text-muted">Aucune vente enregistrée.</td></tr>@endforelse</tbody></table></div></div>
@endsection
