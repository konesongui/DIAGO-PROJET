@extends('admin.layout')
@section('content')
<div class="d-flex justify-content-between align-items-center mb-5"><div><div class="text-uppercase text-muted fs-8 fw-bold">Commercial</div><h2 class="fs-2 fw-bold mb-1">Bons de livraison</h2><p class="text-muted mb-0">Les devis validés apparaissent ici pour préparer leur livraison.</p></div><a href="{{ route('admin.commercial.module', 'devis') }}" class="btn btn-light">Voir les devis</a></div>
@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif
@if($errors->any())<div class="alert alert-danger">{{ $errors->first() }}</div>@endif
<div class="card border-0"><div class="card-header border-0"><h3 class="card-title">Livraisons issues des devis validés</h3></div><div class="card-body table-responsive"><table class="table align-middle"><thead><tr><th>Client</th><th>Code BC client</th><th>Montant TTC</th><th>Type</th><th>Statut</th><th>Action</th></tr></thead><tbody>@forelse($deliveries as $delivery)<tr><td>{{ $delivery->client_name }}</td><td>{{ $delivery->order->customer_order_code ?: '-' }}</td><td>{{ number_format($delivery->order->total_ttc,0,',',' ') }} XOF</td><td>{{ $delivery->delivery_type === 'complete' ? 'Complète' : 'Partielle' }}</td><td><span class="badge badge-light-{{ $delivery->status === 'validated' ? 'success' : 'warning' }}">{{ $delivery->status === 'validated' ? 'Validée' : 'En attente' }}</span></td><td>@if($delivery->status === 'pending_validation')<form method="POST" action="{{ route('admin.commercial.deliveries.validate', $delivery) }}" class="d-flex gap-2">@csrf<select name="delivery_type" class="form-select form-select-sm"><option value="partial">Livraison partielle</option><option value="complete">Livraison complète</option></select><button class="btn btn-sm btn-success">Valider</button></form>@else<span class="text-muted">Facture créée</span>@endif</td></tr>@empty<tr><td colspan="6" class="text-center text-muted">Aucune livraison issue d’un devis validé.</td></tr>@endforelse</tbody></table></div></div>
<div class="text-muted mt-3">Le commercial créateur est conservé avec le devis et transmis jusqu’à la livraison et la facturation.</div>
@endsection
